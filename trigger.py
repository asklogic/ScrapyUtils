from base.engine import single_run, thread_run

import scrapy_config

if __name__ == '__main__':
    # single_run(scrapy_config.xxgx_thread)
    # thread_run(scrapy_config.scjst_thread)
    # single_run(scrapy_config.xxgx_thread)
    # thread_run(scrapy_config.xxgx_thread)
    # single_run(scrapy_config.scjst_thread)
    thread_run(scrapy_config.scjst_thread)

