from base.engine import single_run, thread_run

import scrapy_config

if __name__ == '__main__':
    # single_run(scrapy_config.xxgx_thread)
    # thread_run(scrapy_config.scjst_thread)
    # single_run(scrapy_config.xxgx_thread)
    # thread_run(scrapy_config.xxgx_thread)
    # single_run(scrapy_config.scjst_thread)
    # thread_run(scrapy_config.scjst_thread)
    # single_run(scrapy_config.test_conf)
    # thread_run(scrapy_config.test_conf)

    # single_run(scrapy_config.re_scjst_base)
    # thread_run(scrapy_config.re_scjst_base)

    # single_run(scrapy_config.query_scjst_xmgk)
    # thread_run(scrapy_config.query_scjst_xmgk)

    single_run(scrapy_config.upwork_1)

